/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'pbxt.replicate.delivery',
      'qovqjesrunhjvsth.public.blob.vercel-storage.com',
    ],
  },
};

module.exports = nextConfig;
